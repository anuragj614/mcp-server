services:
  mcp-server:
    build: .
    ports:
      - "8005:8005"
    env_file:
      - .env
    environment:
      - FORCE_COLOR=1
      - TERM=xterm-256color
      - DEBUG=0
      - ENV=production
